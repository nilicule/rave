[project]
name = "raveworld"
version = "0.1.0"
description = "Web-based massively-multiplayer online rave."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
]
